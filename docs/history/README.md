# Historical development documents

This directory contains release notes and development/validation documents from
older Cactri milestones. They are retained for reproducibility and provenance,
but they do not define the current public API or release behavior.

The active release documentation is in the repository root (`README.md`,
`CHANGELOG.md`, `RELEASE_NOTES_0.4.3.md`) and the current source tree.
